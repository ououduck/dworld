# 站点公开访问地址：部署流水线（.github/workflows/deploy.yml）构建时注入。
# 当前版本为预留变量（单页应用无 SSR，页面 URL 仍以 index.html 与 config/site.config.json 为准）。
VITE_SITE_URL=https://www.pldduck.com

# 部署在子路径时填写，例如 GitHub Pages 的 /repo/；根路径部署请留空。
VITE_BASE_PATH=
